[color]
  ui = true
[color "branch"]
  current = yellow reverse
  local = yellow
  remote = green
[color "diff"]
  meta = yellow bold
  frag = magenta bold
  old = red
  new = green
[alias]
  st = status
  br = branch
  co = checkout
  ci = commit
  ft = fetch

  # log
  sl = log --oneline --decorate -25
  l = log --graph --decorate --date=short --format=format:'%C(blue)%ad%Creset %C(yellow)%h%C(green)%d%Creset %C(blue)%s %C(magenta) [%an]%Creset' --all
[push]
  default = current
[advice]
  statusHints = false
[include]
  path = .gitconfig.user
[core]
  editor = vim
  autocrlf = false
  excludesfile = ~/.gitignore
[diff]
  tool = vimdiff
[difftool]
  prompt = false
[merge]
  summary = true
  verbosity = 1
  tool = vimdiff
[mergetool]
  prompt = false
[mergetool "vimdiff"]
  cmd="vim -c 'Gdiff' $MERGED"     # use fugitive.vim for 3-way merge
  keepbackup=false
